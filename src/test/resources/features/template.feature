#Feature: [Título de la funcionalidad]

  #Background: [Pasos que se ejecutan antes de cada escenario]
    #Given (Dado a la acción del usuario)

  #@Tag del Escenario
  #Scenario Outline: [Descripción del flujo principal]
    #When (describe la acción principal del usuario)
    #And (añade pasos adicionales)
    #Then (resultado esperado)"

    #Examples: (permite ejecutar el mismo escenario con múltiples datos usando <> en la variable)
    #Cada fila de la tabla (excepto la cabecera) cuenta como un caso de prueba
    #  | usuario         | clave          |
    #  | "standard_user" | "secret_sauce" |

